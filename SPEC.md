# 擬似マッチング iro — MVP実装仕様
ユーザー指定: 男性・女性を選び、イラストの写真でいいね/スルーを選択。双方からいいねがあったときにマッチするアプリ。追加機能は提案ベース。

## 今回の解釈・MVP
架空の成人キャラクターとの1人用シミュレーション。実在ユーザー同士の出会いやメッセージ送信は実装しない。「相手のいいねはシミュレーション」と明示する。
性別は自分の性別。初版は男性選択→女性3人、女性選択→男性3人。ニックネーム最大16文字、趣味複数選択。
相手は女性: 紬24/カフェ店員(カフェ,読書)、葵26/デザイナー(散歩,写真)、莉央25/広報(旅行,映画)。男性: 悠真27/エンジニア(カフェ,ゲーム)、湊25/映像制作(旅行,写真)、律30/編集者(読書,映画)。
プロフィール文もキャラに合わせて作成。表示年齢は全員20歳以上。

## 判定・状態
セッション開始時に各相手の incomingLike を一度だけ決め、保存する。例: 基本45%+共通趣味1つにつき15%、上限75%。判定はMath.randomを注入可能な純関数にする。
自分の判定 userDecision は undecided/like/pass。マッチは userDecision===like && incomingLike===true のみ。画面再描画・戻る・リロードで相手の判定を再抽選しない。
いいね済みかつ相手のいいねなしは「いいね済み」、マッチ扱いしない。スルーでマッチしない。同じIDは重複追加しない。
1つ戻るで直前の自分の選択を取消し、マッチ一覧/件数も整合させる。相手の判定は維持。
一覧終了時は全員確認済み表示とマッチ一覧への導線。0件の場合も正しい空表示。
性別/趣味の変更・リセットは確認ダイアログ後、全履歴を初期化して新セッション。取消は状態維持。
localStorageはこの端末の状態として名前空間 deai-giji-v1 とスキーマversionを持たせる。JSON破損・アクセス拒否・容量不足は安全に復旧またはメモリ動作へ。保存失敗を成功扱いしない。

## UI・追加機能
スマホ優先の出会いカード。いいね/スルーの大きいボタン、左右スワイプ、キーボード、1つ戻る。
見つける/マッチ一覧/マイプロフィール。年齢・職業・自己紹介・趣味タグ、共通趣味の強調。マッチ成立ダイアログ。
名前など入力はtextContent等で安全に表示。モーダルフォーカス、ラベル、44px程度のタップ領域、reduced-motion、縦スクロールとのスワイプ誤検知防止。
架空・シミュレーション・端末内保存を明示。課金、外部API、ログイン、実在ユーザーチャットはMVP対象外。

## 支給素材・画面案
design/iro/index.html と style.css は作成途中の静的画面案。app.jsは未実装。完成品ではない。
コーラルピンクのブランド、小さなiroロゴ、大きなイラストカード、白系の余白、デスクトップ左ナビ/スマホ下ナビを方向性として採用。必要なレイアウト修正は可。
design/iro/portraits.png は生成済み1536x1024の3列×2行アトラス。各セル512x512。
上段左→右: 紬(茶ロング・カフェ)、葵(黒ボブ・公園)、莉央(茶ポニー・海辺)。
下段左→右: 悠真(黒髪・紺シャツ)、湊(茶髪・クリーム服)、律(眼鏡・緑ジャケット)。
元画像は保存し、必要なら各セルを正確に切り出し、配信用WebPを作成してよい。
重要: 縦長カード全体にbackground-size:300% 200%を直接適用すると顔が縦に歪む。各正方形セルを抽出しimg+object-fit:coverで表示する等で比率を保つこと。
生成ツール: built-in imagegen。プロンプト要約: 成人男女6名、自然な色彩のアニメ寄り手描き、胸上、3x2隙間なし、文字なし。

## 実装と引き継ぎ
このリポジトリ seilap2000s/deai-giji だけで開発。ルート index.html/styles.css/app.js 等でビルド不要の静的アプリとする。既存READMEを内容に更新。
作業ブランチ feature/iro-matching-mvp を使い、用意済みDraft PRを継続して更新。別リポジトリ/別実装PRは作らない。
main直接push、force push、自動マージ、公開設定変更は行わない。実装と確認を完了したらPRをReady for reviewにする。
検証: 判定4通り(自分like/pass×相手true/false)、戻る、重複防止、再読込、破損/拒否storage、男女切替、全件終了、0マッチ、連打、スワイプ、キー、画像失敗、名前のHTML文字。
ブラウザ確認: 390x844、375x667、1280x800でフロー一周。横スクロール、画像歪み/隣セル混入、コンソールエラーを確認。検証環境と実行結果をPRへ。未実施は明記。
